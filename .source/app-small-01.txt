function prospects(){return `<header><div><p class="eyebrow">Sales pipeline</p><h1>Prospects</h1></div><button id="addProspect">Add prospect</button></header><section class="cards">${state.data.prospects.map(p=>`<article class="panel"><div class="row"><h3>${esc(p.businessName)}</h3><span class="pill">${esc(p.status)}</span></div><p>${esc(p.industry)} · ${esc(p.city)}</p><div class="score">${p.score??'—'}<small>AI opportunity score</small></div><p>Potential missed revenue: <b>${p.estimatedMissedRevenue?money(p.estimatedMissedRevenue):'Run audit'}</b></p><div class="actions"><button data-audit="${p.id}">Audit</button><button data-convert="${p.id}" class="secondary">Convert</button></div></article>`).join('')||'<p>No prospects yet.</p>'}</section>`}
function clients(){return `<header><div><p class="eyebrow">Customer operations</p><h1>Clients</h1></div></header><section class="cards">${state.data.clients.map(c=>`<article class="panel"><div class="row"><h3>${esc(c.businessName)}</h3><span class="pill ${c.status==='Live'?'ok':''}">${esc(c.status)}</span></div><p>${esc(c.plan)} · ${money(c.monthlyPrice)}/month</p><progress value="${c.launchProgress}" max="100"></progress><div class="actions"><button data-client="${c.id}">Manage</button><button data-launch="${c.id}" class="secondary">Launch</button></div></article>`).join('')}</section>`}
function agents(){return `<header><div><p class="eyebrow">Grounded AI workforce</p><h1>AI Agents</h1></div></header><section class="cards">${state.data.agents.map(a=>`<article class="panel"><div class="row"><h3>${esc(a.name)}</h3><span class="pill ${a.status==='Active'?'ok':'warn'}">${esc(a.status)}</span></div><p>${esc(a.role)} · ${esc(a.model)}</p><p>${esc(a.objective)}</p><div class="actions"><button data-test="${a.id}">Test intelligence</button><button data-agent="${a.id}" class="secondary">Configure</button></div></article>`).join('')}</section>`}
function knowledge(){return `<header><div><p class="eyebrow">Approved source of truth</p><h1>Knowledge</h1></div><button id="addKnowledge">Add knowledge</button></header><section class="panel"><p>The agent may answer business facts only from these approved documents. Unknown facts are escalated instead of guessed.</p>${state.data.knowledgeDocuments.map(d=>`<div class="row line"><div><b>${esc(d.title)}</b><small>${d.chunks} chunks · ${esc(d.sourceType)}</small></div><button data-delete-doc="${d.id}" class="danger">Delete</button></div>`).join('')||'<p>No documents.</p>'}</section>`}
function inbox(){return `<header><div><p class="eyebrow">Human review queue</p><h1>Inbox</h1></div></header><section class="grid2"><article class="panel list">${state.data.conversations.map(c=>`<button data-conv="${c.id}" class="conversation ${c.requiresHandoff?'urgent':''}"><b>${esc(c.contactName)}</b><span>${esc(c.channel)} · ${esc(c.status)}</span><small>${esc(c.summary)}</small></button>`).join('')||'<p>No conversations.</p>'}</article><article class="panel" id="thread"><h2>Select a conversation</h2></article></section>`}
function billing(){const revenue=state.data.clients.reduce((s,c)=>s+c.monthlyPrice,0);return `<header><div><p class="eyebrow">Subscriptions and margin</p><h1>Billing</h1></div></header><section class="metrics">${card('Client MRR',money(revenue))}${card('Estimated AI cost',money(state.data.dashboard.metrics.aiCost))}${card('Gross margin',`${state.data.dashboard.metrics.grossMargin}%`)}</section><section class="panel">${state.data.clients.map(c=>`<div class="row line"><div><b>${esc(c.businessName)}</b><small>${esc(c.plan)} · ${esc(c.subscriptionStatus)}</small></div><div class="actions"><button data-checkout="${c.id}">Checkout</button><button data-portal="${c.id}" class="secondary">Portal</button></div></div>`).join('')}</section>`}
