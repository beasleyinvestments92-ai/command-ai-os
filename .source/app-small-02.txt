function settings(){const b=state.data.brand,s=state.data.settings;return `<header><div><p class="eyebrow">White label and providers</p><h1>Settings</h1></div></header><section class="grid2"><form id="brandForm" class="panel"><h2>Brand</h2><label>Name<input name="name" value="${esc(b.name)}"></label><label>Tagline<input name="tagline" value="${esc(b.tagline)}"></label><label>Accent<input name="accent" type="color" value="${esc(b.accent)}"></label><label>Support email<input name="supportEmail" value="${esc(b.supportEmail)}"></label><button>Save brand</button></form><form id="settingsForm" class="panel"><h2>AI policy</h2><label>Timezone<input name="timezone" value="${esc(s.timezone)}"></label><label class="check"><input name="simulationMode" type="checkbox" ${s.simulationMode?'checked':''}> Simulation mode</label><label>Allowed widget origins<textarea name="allowedWidgetOrigins">${esc((s.allowedWidgetOrigins||[]).join('\n'))}</textarea></label><button>Save policy</button></form><article class="panel"><h2>Integrations</h2><div id="integrations">Loading…</div><div class="actions"><button id="testOpenAI">Test OpenAI</button><button id="testGhl" class="secondary">Test HighLevel</button></div></article><form id="ghlForm" class="panel"><h2>HighLevel</h2><label>Access token<input name="accessToken" type="password"></label><label>Location ID<input name="locationId"></label><label>Calendar ID<input name="calendarId"></label><button>Encrypt and save</button></form></section>`}
function release(){return `<header><div><p class="eyebrow">Go-live control</p><h1>Release Readiness</h1></div><button id="runRelease">Run checks</button></header><section id="releaseBody" class="panel"><p>Run the release gate before accepting paying customers.</p></section>`}
function render(){const views={dashboard,prospects,clients,agents,knowledge,inbox,billing,settings,release};shell(views[state.route]());bind()}
async function reload(msg){state.data=await api('/api/bootstrap');if(msg)toast(msg);render()}
function formObj(form){return Object.fromEntries(new FormData(form).entries())}
function bind(){
 $('#refresh')&&( $('#refresh').onclick=()=>reload('Dashboard refreshed'));
 $('#addProspect')&&( $('#addProspect').onclick=()=>modal('Add prospect',`<form id="modalForm"><label>Business<input name="businessName" required></label><label>Industry<input name="industry" value="Local Service"></label><label>City<input name="city"></label><label>Phone<input name="phone"></label><label>Email<input name="email"></label><label>Website<input name="website"></label><button>Add prospect</button></form>`));
 $$('[data-audit]').forEach(b=>b.onclick=async()=>{await api(`/api/prospects/${b.dataset.audit}/audit`,{method:'POST',body:{}});reload('Audit complete')});
 $$('[data-convert]').forEach(b=>b.onclick=async()=>{await api(`/api/prospects/${b.dataset.convert}/convert`,{method:'POST',body:{plan:'Growth'}});reload('Prospect converted')});
 $$('[data-launch]').forEach(b=>b.onclick=async()=>{await api(`/api/clients/${b.dataset.launch}/launch`,{method:'POST',body:{goLive:true}});reload('Client launched')});
 $$('[data-client]').forEach(b=>b.onclick=()=>{const c=state.data.clients.find(x=>x.id===b.dataset.client);modal('Manage client',`<form id="clientForm" data-id="${c.id}"><label>Business<input name="businessName" value="${esc(c.businessName)}"></label><label>Industry<input name="industry" value="${esc(c.industry)}"></label><label>Plan<input name="plan" value="${esc(c.plan)}"></label><label>Monthly price<input name="monthlyPrice" type="number" value="${c.monthlyPrice}"></label><label>Timezone<input name="timezone" value="${esc(c.timezone)}"></label><button>Save</button></form>`) });
 $$('[data-test]').forEach(b=>b.onclick=()=>modal('Agent intelligence test',`<form id="testForm" data-id="${b.dataset.test}"><label>Question<textarea name="message" required>What services do you provide?</textarea></label><button>Run grounded test</button></form><pre id="testResult"></pre>`));
