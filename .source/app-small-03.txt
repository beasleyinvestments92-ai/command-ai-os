 $$('[data-agent]').forEach(b=>b.onclick=()=>{const a=state.data.agents.find(x=>x.id===b.dataset.agent);modal('Configure agent',`<form id="agentForm" data-id="${a.id}"><label>Name<input name="name" value="${esc(a.name)}"></label><label>Status<select name="status"><option ${a.status==='Active'?'selected':''}>Active</option><option ${a.status==='Draft'?'selected':''}>Draft</option></select></label><label>Tone<textarea name="tone">${esc(a.tone)}</textarea></label><label>Objective<textarea name="objective">${esc(a.objective)}</textarea></label><label>Guardrails<textarea name="guardrails">${esc(a.guardrails)}</textarea></label><label>Handoff number<input name="handoffNumber" value="${esc(a.handoffNumber)}"></label><button>Save agent</button></form>`) });
 $('#addKnowledge')&&( $('#addKnowledge').onclick=()=>modal('Add approved knowledge',`<form id="knowledgeForm"><label>Client<select name="clientId">${state.data.clients.map(c=>`<option value="${c.id}">${esc(c.businessName)}</option>`)}</select></label><label>Agent<select name="agentId"><option value="">All client agents</option>${state.data.agents.map(a=>`<option value="${a.id}">${esc(a.name)}</option>`)}</select></label><label>Title<input name="title" required></label><label>Approved facts<textarea name="content" rows="10" required></textarea></label><button>Index knowledge</button></form>`));
 $$('[data-delete-doc]').forEach(b=>b.onclick=async()=>{await api(`/api/knowledge/${b.dataset.deleteDoc}`,{method:'DELETE'});reload('Knowledge removed')});
 $$('[data-conv]').forEach(b=>b.onclick=()=>showThread(b.dataset.conv));
 $$('[data-checkout]').forEach(b=>b.onclick=async()=>{const x=await api(`/api/billing/clients/${b.dataset.checkout}/checkout`,{method:'POST',body:{}});location.href=x.url});
 $$('[data-portal]').forEach(b=>b.onclick=async()=>{const x=await api(`/api/billing/clients/${b.dataset.portal}/portal`,{method:'POST',body:{}});location.href=x.url});
 $('#brandForm')&&( $('#brandForm').onsubmit=async e=>{e.preventDefault();await api('/api/brand',{method:'PUT',body:formObj(e.target)});reload('Brand saved')});
 $('#settingsForm')&&( $('#settingsForm').onsubmit=async e=>{e.preventDefault();const x=formObj(e.target);x.simulationMode=e.target.simulationMode.checked;x.allowedWidgetOrigins=x.allowedWidgetOrigins.split(/\n+/).map(v=>v.trim()).filter(Boolean);await api('/api/settings',{method:'PUT',body:x});reload('AI policy saved')});
 $('#ghlForm')&&( $('#ghlForm').onsubmit=async e=>{e.preventDefault();await api('/api/integrations/highlevel',{method:'POST',body:formObj(e.target)});toast('HighLevel credentials encrypted and saved')});
 $('#testOpenAI')&&( $('#testOpenAI').onclick=async()=>{const x=await api('/api/integrations/openai/test',{method:'POST',body:{}});toast(x.message,!x.connected)});
 $('#testGhl')&&( $('#testGhl').onclick=async()=>{const x=await api('/api/integrations/highlevel/test',{method:'POST',body:{}});toast(x.message||'HighLevel test completed',!x.connected)});
 $('#runRelease')&&( $('#runRelease').onclick=async()=>{const x=await api('/api/release-readiness');$('#releaseBody').innerHTML=`<h2>${x.ready?'Release gate passed':'Release blocked'}</h2>${x.checks.map(c=>`<div class="row line"><span>${esc(c.label)}</span><b class="pill ${c.ready?'ok':'warn'}">${c.ready?'Ready':'Required'}</b></div>`).join('')}`});
 api('/api/integrations').then(x=>{$('#integrations')&&($('#integrations').innerHTML=Object.entries(x).map(([k,v])=>`<div class="row line"><span>${esc(k)}</span><b class="pill ${v.configured?'ok':'warn'}">${v.configured?'Connected':'Not configured'}</b></div>`).join(''))}).catch(()=>{});
}
